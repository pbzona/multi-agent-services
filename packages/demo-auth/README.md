# `@repo/demo-auth`

Shared demo-session and commerce-token helpers for the reference app.

The package is for local and preview demonstrations. It is not an identity
provider. Replace the fixed personas and shared HMAC signing model before using
the application with real users or data.
